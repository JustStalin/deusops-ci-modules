# deusops-ci-modules

Reusable GitLab CI templates for DeusOps labs.

## Templates

- `templates/docker-build.yml` - smoke test and docker image build/push jobs.
- `templates/docker-deploy.yml` - deploy job for docker compose stacks.
